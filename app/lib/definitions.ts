export type Book = {
  cover_image: string,
  title: string,
  author: string,
  description: string,
  last_update: string,
}

export type User = {
  user_id: string,
  password: string,
  profile_image: string,
  
}